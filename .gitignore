node_modules
dist
.env
*.log
token.json
credentials.json
.gemini/

# Claude Code internal tooling (worktrees, etc.)
.claude/

# Local agent context — personal, not for sharing
.agents/

# Generated files
weekly-parenting-plan.md
data/

# Personal inputs (resume, JDs, notes) — not for sharing
inputs/

# Personal config (names, communication style) — copy from user.config.example.json
user.config.json

# Personal planning artifact — local only
PLAN.md

# Claude Code internal files
skills-lock.json

# Serena language-server index (auto-provisioned per worktree)
.serena/
